# DeepSeek API key for the AI model
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# Telegram Bot token (get from @BotFather on Telegram)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Webhook URL - your VPS domain or IP with HTTPS (include port if not 443)
# Example: https://your-domain.com or https://123.45.67.89:8443
WEBHOOK_URL=https://your-domain.com

# Webhook port (default: 8443)
# Telegram supports ports: 443, 80, 88, 8443
WEBHOOK_PORT=8443

# SSL certificate and private key paths (for self-signed certs / direct HTTPS)
# Generate with:
#   openssl req -newkey rsa:2048 -sha256 -nodes \
#     -keyout private.key -x509 -days 3650 \
#     -out cert.pem -subj "/CN=YOUR_VPS_IP"
# If using a reverse proxy (nginx/Caddy) for TLS, leave these empty.
WEBHOOK_SSL_CERT=/path/to/cert.pem
WEBHOOK_SSL_KEY=/path/to/private.key
